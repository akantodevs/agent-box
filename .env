# Compose project name. docker compose reads this automatically for the host-side
# project name, and it is also injected into the agent container (see docker-compose.yml)
# so the in-container agent's `docker compose` commands target the same stack.
# For a real project, set this to that project's name.
COMPOSE_PROJECT_NAME=agent-box
